[package]
name = "arrow_layout"
version = "25.0.0"
edition = "2021"
description = "Shared zero-copy Arrow page layout contract for import and slot_encoder"
readme = "README.md"

[lib]
crate-type = ["lib"]
doctest = false

[dependencies]
arrow-schema = { workspace = true }
thiserror = "2.0"
